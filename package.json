{
  "name": "justwords-content-checker-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Local Playwright helper for the Justwords Content Quality Checker — Google search + competitor page reader, no API key. Also serves index.html so the tool runs same-origin.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "playwright install chromium",
    "test": "node tests/regression.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
